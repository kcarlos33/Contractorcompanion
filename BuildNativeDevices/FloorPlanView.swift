import SwiftUI
import RoomPlan

struct FloorPlanView: View {
    let capturedRoom: CapturedRoom

    var body: some View {
        Text("2D Floor Plan View (Implementation Pending)")
            .padding()
    }
}